diff --git a/SETUP/config/Makefile b/SETUP/config/Makefile
index 8889afe..896ba9e 100644
--- a/SETUP/config/Makefile
+++ b/SETUP/config/Makefile
@@ -16,6 +16,15 @@ WARNFLAGS = -Wall
 
 LDFLAGS = -isysroot $(HOST_SDKROOT)
 
+# Linux host tool compat: K&R C, BSD types from sys/types.h via _DEFAULT_SOURCE
+# NOTE: -O1 (not -O0) is required to avoid segfaults in K&R pointer code
+UNAME_S := $(shell uname -s)
+ifeq ($(UNAME_S),Linux)
+override CFLAGS := -O1 -g -I$(SOURCE) -I. -std=gnu99 -D_DEFAULT_SOURCE -include sys/sysmacros.h -Wno-implicit-int -Wno-implicit-function-declaration -Wno-deprecated-non-prototype -Wno-pointer-to-int-cast
+endif
+
+# On Linux, config must be pre-built (below): K&R C + BSD-isms
+# segfault at -O0 with modern Clang. See doconf_patches.sh.
 config: $(OBJS)
 	$(_v)$(HOST_CC) $(LDFLAGS) -o $@ $^
 	@echo HOST_LD $@
diff --git a/SETUP/decomment/Makefile b/SETUP/decomment/Makefile
index 05cf5b8..d00540a 100644
--- a/SETUP/decomment/Makefile
+++ b/SETUP/decomment/Makefile
@@ -15,6 +15,12 @@ WARNFLAGS = -Wall
 
 LDFLAGS = -isysroot $(HOST_SDKROOT)
 
+# Linux host tool compat: getc_unlocked/putchar_unlocked need _DEFAULT_SOURCE
+UNAME_S := $(shell uname -s)
+ifeq ($(UNAME_S),Linux)
+override CFLAGS += -std=gnu99 -D_DEFAULT_SOURCE -Wno-implicit-int -Wno-implicit-function-declaration
+endif
+
 decomment: $(OBJS)
 	$(_v)$(HOST_CC) $(LDFLAGS) -o $@ $^
 	@echo HOST_LD $@
diff --git a/SETUP/md/Makefile b/SETUP/md/Makefile
index b2741a7..1e8623e 100644
--- a/SETUP/md/Makefile
+++ b/SETUP/md/Makefile
@@ -15,6 +15,12 @@ WARNFLAGS = -Wall
 
 LDFLAGS = -isysroot $(HOST_SDKROOT)
 
+# Linux host tool compat: K&R C, S_IFMT requires _DEFAULT_SOURCE
+UNAME_S := $(shell uname -s)
+ifeq ($(UNAME_S),Linux)
+override CFLAGS += -std=gnu99 -D_DEFAULT_SOURCE -include sys/sysmacros.h -Wno-implicit-int -Wno-implicit-function-declaration -Wno-deprecated-non-prototype
+endif
+
 md: $(OBJS)
 	$(_v)$(HOST_CC) $(LDFLAGS) -o $@ $^
 	@echo HOST_LD $@
diff --git a/libkern/kmod/c_start.c b/libkern/kmod/c_start.c
index a859e22..8cc7c1a 100644
--- a/libkern/kmod/c_start.c
+++ b/libkern/kmod/c_start.c
@@ -51,7 +51,10 @@ extern kmod_info_t KMOD_INFO_NAME;
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ kern_return_t _start(kmod_info_t *ki, void *data)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+kern_return_t _start(kmod_info_t *ki, void *data)
 {
     if (_realmain)
         return (*_realmain)(ki, data);
@@ -61,21 +64,30 @@ __private_extern__ kern_return_t _start(kmod_info_t *ki, void *data)
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ const char * OSKextGetCurrentIdentifier(void)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+const char * OSKextGetCurrentIdentifier(void)
 {
     return KMOD_INFO_NAME.name;
 }
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ const char * OSKextGetCurrentVersionString(void)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+const char * OSKextGetCurrentVersionString(void)
 {
     return KMOD_INFO_NAME.version;
 }
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ OSKextLoadTag OSKextGetCurrentLoadTag(void)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+OSKextLoadTag OSKextGetCurrentLoadTag(void)
 {
     return (OSKextLoadTag)KMOD_INFO_NAME.id;
 }
diff --git a/libkern/kmod/c_stop.c b/libkern/kmod/c_stop.c
index d718160..8276d10 100644
--- a/libkern/kmod/c_stop.c
+++ b/libkern/kmod/c_stop.c
@@ -47,7 +47,10 @@
 // These global symbols will be defined by CreateInfo script's info.c file.
 extern kmod_stop_func_t *_antimain;
 
-__private_extern__ kern_return_t _stop(kmod_info_t *ki, void *data)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+kern_return_t _stop(kmod_info_t *ki, void *data)
 {
     if (_antimain)
         return (*_antimain)(ki, data);
diff --git a/libkern/kmod/cplus_start.c b/libkern/kmod/cplus_start.c
index 31f2704..9644e52 100644
--- a/libkern/kmod/cplus_start.c
+++ b/libkern/kmod/cplus_start.c
@@ -52,7 +52,11 @@
 #include <libkern/OSKextLib.h>
 
 asm(".constructors_used = 0");
+#if defined(__ELF__)
+asm(".hidden .constructors_used");
+#else
 asm(".private_extern .constructors_used");
+#endif
 
 // This global symbols will be defined by CreateInfo script's info.c file.
 extern kmod_start_func_t *_realmain;
@@ -64,7 +68,10 @@ extern kern_return_t OSRuntimeFinalizeCPP(kmod_info_t *ki, void *data);
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ kern_return_t _start(kmod_info_t *ki, void *data)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+kern_return_t _start(kmod_info_t *ki, void *data)
 {
     kern_return_t result = OSRuntimeInitializeCPP(ki, data);
 
@@ -83,21 +90,30 @@ __private_extern__ kern_return_t _start(kmod_info_t *ki, void *data)
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ const char * OSKextGetCurrentIdentifier(void)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+const char * OSKextGetCurrentIdentifier(void)
 {
     return KMOD_INFO_NAME.name;
 }
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ const char * OSKextGetCurrentVersionString(void)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+const char * OSKextGetCurrentVersionString(void)
 {
     return KMOD_INFO_NAME.version;
 }
 
 /*********************************************************************
 *********************************************************************/
-__private_extern__ OSKextLoadTag OSKextGetCurrentLoadTag(void)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+OSKextLoadTag OSKextGetCurrentLoadTag(void)
 {
     return (OSKextLoadTag)KMOD_INFO_NAME.id;
 }
diff --git a/libkern/kmod/cplus_stop.c b/libkern/kmod/cplus_stop.c
index 2b2bdc6..06490c4 100644
--- a/libkern/kmod/cplus_stop.c
+++ b/libkern/kmod/cplus_stop.c
@@ -51,7 +51,11 @@
 #include <mach/mach_types.h>
 
 asm(".destructors_used = 0");
+#if defined(__ELF__)
+asm(".hidden .destructors_used");
+#else
 asm(".private_extern .destructors_used");
+#endif
 
 // Functions defined in libkern/c++/OSRuntime.cpp
 extern kern_return_t OSRuntimeFinalizeCPP(kmod_info_t *ki, void *data);
@@ -59,7 +63,10 @@ extern kern_return_t OSRuntimeFinalizeCPP(kmod_info_t *ki, void *data);
 // This global symbols will be defined by CreateInfo script's info.c file.
 extern kmod_stop_func_t *_antimain;
 
-__private_extern__ kern_return_t _stop(kmod_info_t *ki, void *data)
+#if defined(__ELF__)
+__attribute__((visibility("hidden")))
+#endif
+kern_return_t _stop(kmod_info_t *ki, void *data)
 {
     kern_return_t result = KERN_SUCCESS;
 
diff --git a/libkern/libkern/OSAtomic.h b/libkern/libkern/OSAtomic.h
index 8499c69..dd021d1 100644
--- a/libkern/libkern/OSAtomic.h
+++ b/libkern/libkern/OSAtomic.h
@@ -644,10 +644,10 @@ static inline void OSMemoryBarrier(void) {
 #if defined(__arm__)
 #ifndef __LP64__
 static inline void OSMemoryBarrier(void) {
-#ifdef _ARM_ARCH_7
+#if defined(_ARM_ARCH_7) || defined(__ARM_ARCH_7__) || defined(__ARM_ARCH_7A__)
 	__asm__ volatile("dsb" ::: "memory");
 #else
-	UInt32 temp = 0;
+	int temp;
 	__asm__ volatile("mcr p15, 0, %0, c7, c10, 4" : : "r" (temp));
 #endif
 }
diff --git a/machine_configuration b/machine_configuration
index 20e71bb..16ce80e 100644
--- a/machine_configuration
+++ b/machine_configuration
@@ -19,3 +19,6 @@ release arm s5l8930x
 release arm s5l8920x
 release arm s5l8922x
 release arm omap3430_rx51
+debug arm64 msm8916_harpia
+development arm64 msm8916_harpia
+release arm64 msm8916_harpia
diff --git a/makedefs/MakeInc.cmd b/makedefs/MakeInc.cmd
index 723aebf..dddce23 100644
--- a/makedefs/MakeInc.cmd
+++ b/makedefs/MakeInc.cmd
@@ -226,37 +226,19 @@ else ifeq ($(UNAME_S),Linux)
 	# CC/CXX get defined by make(1) by default, so we can't check them
 	# against the empty string to see if they haven't been set
 	##
-	ifeq ($(origin CC),default)
-		export CC := $(shell $(XCRUN) -sdk $(SDKROOT) -find clang)
-	endif
-	ifeq ($(origin CXX),default)
-		export CXX := $(shell $(XCRUN) -sdk $(SDKROOT) -find clang++)
-	endif
+	export CC := clang
+	export CXX := clang++
 
 	##
 	# Toolchain tools for cross building
 	##
-	ifeq ($(LD),)
-		export LD := $(shell $(XCRUN) -sdk $(SDKROOT) -find ld)
-	endif
-	ifeq ($(AS),)
-		export AS := $(shell $(XCRUN) -sdk $(SDKROOT) -find as)
-	endif
-	ifeq ($(STRIP),)
-		export STRIP := $(shell $(XCRUN) -sdk $(SDKROOT) -find strip)
-	endif
-	ifeq ($(LIPO),)
-		export LIPO := $(shell $(XCRUN) -sdk $(SDKROOT) -find lipo)
-	endif
-	ifeq ($(LIBTOOL),)
-		export LIBTOOL := $(shell $(XCRUN) -sdk $(SDKROOT) -find libtool)
-	endif
-	ifeq ($(NM),)
-		export NM := $(shell $(XCRUN) -sdk $(SDKROOT) -find nm)
-	endif
-	ifeq ($(NMEDIT),)
-		export NMEDIT := $(shell $(XCRUN) -sdk $(SDKROOT) -find nmedit)
-	endif
+	export LD := arm-apple-darwin11-ld
+	export AS := arm-apple-darwin11-as
+	export STRIP := arm-apple-darwin11-strip
+	export LIPO := arm-apple-darwin11-lipo
+	export LIBTOOL := arm-apple-darwin11-libtool
+	export NM := arm-apple-darwin11-nm
+	export NMEDIT := arm-apple-darwin11-nmedit
 
 	# XXX May need this later
 	CTFINSERT := /bin/true
@@ -264,33 +246,19 @@ else ifeq ($(UNAME_S),Linux)
 	##
 	# Misc. tools for building
 	##
-	ifeq ($(MIG),)
-		export MIG := $(shell $(XCRUN) -sdk $(SDKROOT) -find mig)
-	endif
-	ifeq ($(MIGCC),)
-		export MIGCC := $(CC)
-	endif
-	ifeq ($(UNIFDEF),)
-		export UNIFDEF := $(shell $(XCRUN) -sdk $(SDKROOT) -find unifdef)
-	endif
-	ifeq ($(DSYMUTIL),)
-		export DSYMUTIL := /bin/true
-	endif
-	ifeq ($(CTFCONVERT),)
-		export CTFCONVERT := /bin/true
-	endif
-	ifeq ($(CTFMERGE),)
-		export CTFMERGE :=  /bin/true
-	endif
-	ifeq ($(CTFSCRUB),)
-		export CTFSCRUB := /bin/true -r
-	endif
+	export MIG := mig
+	export MIGCC := clang
+	export UNIFDEF := unifdef
+	export DSYMUTIL := /bin/true
+	export CTFCONVERT := /bin/true
+	export CTFMERGE :=  /bin/true
+	export CTFSCRUB := /bin/true -r
 
 	##
 	# Other special tools
 	##
-	SEG_HACK := $(shell $(XCRUN) -sdk $(SDKROOT) -find setsegname)
-	KEXT_CREATE_SYMBOL_SET := $(shell $(XCRUN) -sdk $(SDKROOT) -find kextsymboltool)
+	SEG_HACK := arm-apple-darwin11-seg_hack
+	KEXT_CREATE_SYMBOL_SET := $(OBJROOT)/SETUP/kextsymboltool/kextsymboltool
 	DECOMMENT := $(OBJROOT)/SETUP/decomment/decomment
 	NEWVERS = $(SRCROOT)/config/newvers.pl
 	MD := $(OBJROOT)/SETUP/md/md
@@ -299,13 +267,13 @@ else ifeq ($(UNAME_S),Linux)
 	# Commands to generate host binaries.
 	###
 	ifeq ($(HOST_CC),)
-		export HOST_CC := $(shell $(XCRUN) -sdk $(HOST_SDKROOT) -find clang)
+		export HOST_CC := $(shell tmp=$$($(XCRUN) -sdk $(SDKROOT) -find clang 2>/dev/null); if [ -z "$$tmp" ]; then echo cc; else echo "$$tmp"; fi)
 	endif
 	ifeq ($(HOST_FLEX),)
-		export HOST_FLEX := $(shell $(XCRUN) -sdk $(HOST_SDKROOT) -find flex)
+		export HOST_FLEX := $(shell tmp=$$($(XCRUN) -sdk $(SDKROOT) -find flex 2>/dev/null); if [ -z "$$tmp" ]; then echo flex; else echo "$$tmp"; fi)
 	endif
 	ifeq ($(HOST_BISON),)
-		export HOST_BISON := $(shell $(XCRUN) -sdk $(HOST_SDKROOT) -find bison)
+		export HOST_BISON := $(shell tmp=$$($(XCRUN) -sdk $(SDKROOT) -find bison 2>/dev/null); if [ -z "$$tmp" ]; then echo bison; else echo "$$tmp"; fi)
 	endif
 	ifeq ($(HOST_CODESIGN),)
 		export HOST_CODESIGN := /bin/true
diff --git a/makedefs/MakeInc.def b/makedefs/MakeInc.def
index ee67e78..7c1e96a 100644
--- a/makedefs/MakeInc.def
+++ b/makedefs/MakeInc.def
@@ -121,8 +121,9 @@ endif
 export SUPPORTED_I386_MACHINE_CONFIGS := DEFAULT
 export SUPPORTED_X86_64_MACHINE_CONFIGS := DEFAULT
 export SUPPORTED_ARM_MACHINE_CONFIGS := S5L8920X S5L8922X S5L8930X ARMPBA8 SUN4I OMAP3530 OMAP335X RASPBERRYPI \
-					OMAP3430_RX51 ARM_RVEB_V6 S5L8720X S5L8900XRB MSM8960_TOUCHPAD IMX53
-export SUPPORTED_ARM64_MACHINE_CONFIGS := DEFAULT VEXPRESS 
+					OMAP3430_RX51 ARM_RVEB_V6 S5L8720X S5L8900XRB MSM8960_TOUCHPAD IMX53 \
+					MSM8916_HARPIA
+export SUPPORTED_ARM64_MACHINE_CONFIGS := DEFAULT VEXPRESS MSM8916_HARPIA 
 
 #
 # Target configuration options.  NOTE - target configurations will 
@@ -311,6 +312,7 @@ ARCH_FLAGS_ARM64	= -arch arm64
 
 ARCH_FLAGS_ARM		  = $($(addsuffix $(MACHINE_CONFIG),ARCH_FLAGS_ARM_))
 
+ARCH_FLAGS_ARM_MSM8916_HARPIA	= -arch armv7
 ARCH_FLAGS_ARM_S5L8930X	= -arch armv7
 ARCH_FLAGS_ARM_S5L8922X	= -arch armv7
 ARCH_FLAGS_ARM_S5L8920X	= -arch armv7
@@ -324,6 +326,8 @@ ARCH_FLAGS_ARM_MSM8960_TOUCHPAD	= -arch armv7
 ARCH_FLAGS_ARM_RASPBERRYPI	= -arch armv6
 ARCH_FLAGS_ARM_ARM_RVEB_V6	= -arch armv6
 ARCH_FLAGS_ARM_IMX53		= -arch armv7
+ARCH_FLAGS_ARM_MOTO_MSM8916 = -arch armv7
+ARCH_FLAGS_ARM_MSM8916_HARPIA = -arch armv7
 
 #
 # Default CFLAGS
@@ -333,7 +337,7 @@ ifeq ($(UNAME_S),Linux)
 	# XXX Fix this.
 	ifeq ($(word 2, $(TARGET_CONFIGS_UC)),ARM)
 		BUILD_NO_DEBUG := 1
-        RC_CFLAGS := -D__arm__ -DARM
+		RC_CFLAGS := -D__arm__ -DARM -D__APPLE__
 	endif
 endif
 
@@ -423,6 +427,11 @@ export CFLAGS 	= $(CFLAGS_GEN) \
 
 OTHER_CXXFLAGS	=
 
+ifeq ($(UNAME_S),Linux)
+OTHER_CXXFLAGS += -std=gnu++11 -Wno-register
+OTHER_CFLAGS += -Wno-cast-align -Wno-int-to-pointer-cast -Wno-pointer-to-int-cast
+endif
+
 CXXFLAGS_GEN  = -fapple-kext $(OTHER_CXXFLAGS)
 
 CXXFLAGS      = $(CXXFLAGS_GEN) \
diff --git a/makedefs/MakeInc.dir b/makedefs/MakeInc.dir
index c615728..b433609 100644
--- a/makedefs/MakeInc.dir
+++ b/makedefs/MakeInc.dir
@@ -490,8 +490,6 @@ all_embedded all_devicemap: all
 #
 # Build all architectures for all Configuration/Architecture options
 #
-do_build_all:
-
 BUILD_ALL_SUBDIRS_TARGETS = $(addprefix build_all_,$(COMP_SUBDIRS) $($(addprefix COMP_SUBDIRS_, $(ARCH_CONFIG))))
 
 .PHONY: $(BUILD_ALL_SUBDIRS_TARGETS)
diff --git a/osfmk/arm/PlatformConfigs.h b/osfmk/arm/PlatformConfigs.h
index f7247bc..9e1f8f5 100644
--- a/osfmk/arm/PlatformConfigs.h
+++ b/osfmk/arm/PlatformConfigs.h
@@ -30,6 +30,12 @@
 #ifndef _PLATFORMCONFIGS_H_
 #define _PLATFORMCONFIGS_H_
 
+/* ARMv8-A Cortex-A53 platforms (32-bit mode). */
+
+#ifdef BOARD_CONFIG_MSM8916_HARPIA
+#define __ARM_PROCESSOR_CLASS_CORTEX_A53__		1
+#endif
+
 /* ARMv7 Cortex-A9 platforms. */
 
 #ifdef BOARD_CONFIG_MSM8960_TOUCHPAD
diff --git a/osfmk/arm/loose_ends.c b/osfmk/arm/loose_ends.c
index 9e34d9e..664814c 100644
--- a/osfmk/arm/loose_ends.c
+++ b/osfmk/arm/loose_ends.c
@@ -436,11 +436,11 @@ kern_return_t copypv(addr64_t src64, addr64_t snk64, unsigned int size, int whic
      * If it's a kernel copy, use ovbcopy. 
      */
     if (!(which & (cppvKmap | cppvPsrc)))
-        copyout(src64, snk64, size);
+        copyout((const void *)(uintptr_t)src64, (user_addr_t)snk64, size);
     else if (!(which & (cppvKmap | cppvPsrc)))
-        copyin(src64, snk64, size);
+        copyin((user_addr_t)src64, (void *)(uintptr_t)snk64, size);
     else
-        bcopy(src64, snk64, size);
+        bcopy((const void *)(uintptr_t)src64, (void *)(uintptr_t)snk64, size);
 
     return KERN_SUCCESS;
 }
diff --git a/osfmk/arm/misc_protos.h b/osfmk/arm/misc_protos.h
index 4cd4969..6290cd7 100644
--- a/osfmk/arm/misc_protos.h
+++ b/osfmk/arm/misc_protos.h
@@ -78,4 +78,11 @@ void cache_initialize(void);
 void get_cachetype_cp15();
 void identify_arm_cpu(void);
 
+extern void machine_startup(void);
+extern void Halt_system(void);
+extern void set_mmu_ttb(uint32_t ttb);
+extern void set_mmu_ttb_alt(uint32_t ttb);
+extern void set_mmu_ttbcr(uint32_t ttbcr);
+extern void flush_mmu_tlb(void);
+
 #endif
diff --git a/osfmk/arm/pal.c b/osfmk/arm/pal.c
index ad03c45..2622801 100644
--- a/osfmk/arm/pal.c
+++ b/osfmk/arm/pal.c
@@ -35,6 +35,10 @@
 #include <machine/pal_routines.h>
 
 /* Serial routines */
+
+extern void serial_putc(char c);
+extern int serial_getc(void);
+
 int pal_serial_init(void)
 {
     return 1;
@@ -43,7 +47,6 @@ int pal_serial_init(void)
 void pal_serial_putc(char c)
 {
     serial_putc(c);
-    return;
 }
 
 int pal_serial_getc(void)
diff --git a/osfmk/arm/pcb.c b/osfmk/arm/pcb.c
index d0596fe..9e0cc17 100644
--- a/osfmk/arm/pcb.c
+++ b/osfmk/arm/pcb.c
@@ -47,6 +47,8 @@
 #include <arm/cpu_data.h>
 #include <arm/misc_protos.h>
 
+extern void ovbcopy(void *from, void *to, vm_size_t len);
+
 thread_t CurrentThread;
 
 /*
diff --git a/osfmk/arm/trap.c b/osfmk/arm/trap.c
index 1c5533e..d928560 100644
--- a/osfmk/arm/trap.c
+++ b/osfmk/arm/trap.c
@@ -52,6 +52,12 @@
 #include <libkern/OSByteOrder.h>
 #include <arm/armops.h>
 
+extern char *proc_name_address(void *);
+extern int proc_pid(void *);
+extern boolean_t kdp_raise_exception(unsigned int, unsigned int, unsigned int, void *);
+extern void __disable_preemption(void);
+extern void __enable_preemption(void);
+
 #define ANSI_COLOR_RED     "\x1b[31m"
 #define ANSI_COLOR_GREEN   "\x1b[32m"
 #define ANSI_COLOR_YELLOW  "\x1b[33m"
@@ -337,7 +343,7 @@ void sleh_abort(void *context, int reason)
 
                             arm_ctx->pc = thread->recover;
                             arm_ctx->cpsr &= ~(1 << 5);
-                            thread->recover = NULL;
+                            thread->recover = 0;
                             return;
                         }
                     }
@@ -601,7 +607,7 @@ void sleh_undef(arm_saved_state_t * state)
          */
         spl_t spl = splhigh();
         thumb_offset = (arm_ctx->cpsr & (1 << 5)) ? 1 : 0;
-        copyin((uint8_t *) (arm_ctx->pc + thumb_offset), &instruction,
+        copyin((user_addr_t)(arm_ctx->pc + thumb_offset), &instruction,
                sizeof(uint32_t));
         splx(spl);
 
diff --git a/osfmk/conf/Makefile.arm b/osfmk/conf/Makefile.arm
index a6d9b9f..ffbad44 100644
--- a/osfmk/conf/Makefile.arm
+++ b/osfmk/conf/Makefile.arm
@@ -9,11 +9,12 @@ export CFLAGS_DEBUGARM = -O0
 export CFLAGS_PROFILEARM = -O0
 
 bcopyinout.o_SFLAGS_ADD += -no-integrated-as
+hw_lock.o_SFLAGS_ADD  += -no-integrated-as
+cswitch.o_SFLAGS_ADD  += -no-integrated-as
 
 ifeq (-arch armv6,$(ARCH_FLAGS_ARM))
 locore.o_SFLAGS_ADD  += -no-integrated-as
 machine_routines_asm.o_SFLAGS_ADD += -no-integrated-as
-hw_lock.o_SFLAGS_ADD  += -no-integrated-as
 cache.o_SFLAGS_ADD += -no-integrated-as
 tlb-arm11.o_SFLAGS_ADD += -no-integrated-as
 endif
diff --git a/osfmk/conf/Makefile.template b/osfmk/conf/Makefile.template
index c39e844..8f1ed62 100644
--- a/osfmk/conf/Makefile.template
+++ b/osfmk/conf/Makefile.template
@@ -120,7 +120,7 @@ genassym.o: $(SOURCE_DIR)/$(COMPONENT)/$(GENASSYM_LOCATION)/genassym.c
 	$(_v)${KCC} $(subst -flto,,${CFLAGS}) -MD ${_HOST_EXTRA_CFLAGS} -S -o ${@} -c ${INCFLAGS} $<
 
 assym.s: genassym.o
-	$(_v)sed -e '/#DEFINITION#/!d' -e 's/^.*#DEFINITION#//' -e 's/\$$//' -e 'p' -e 's/#//2' -e 's/[^A-Za-z0-9_]*\([A-Za-z0-9_]*\)/ \1_NUM/2' genassym.o > ${@}
+	$(_v)perl -nle 'if(/DEFINITION#/){/define\s+(\S+)\s+#(\S+)/o; print "#define $$1 $$2"}' genassym.o > ${@}
 
 ${SOBJS}: assym.s
 
diff --git a/osfmk/conf/files.arm64 b/osfmk/conf/files.arm64
index bd8036e..b04db2e 100644
--- a/osfmk/conf/files.arm64
+++ b/osfmk/conf/files.arm64
@@ -13,6 +13,8 @@ osfmk/console/video_console.c        optional  vc device-driver
 osfmk/console/arm/video_scroll.c	optional vc device-driver
 osfmk/console/arm/serial_console.c  optional vc
 
+osfmk/arm64/conf.c		standard
+
 osfmk/arm64/memcmp.s		standard
 osfmk/arm64/memmove.s		standard
 osfmk/arm64/memset.s		standard
@@ -41,4 +43,3 @@ osfmk/arm64/pmap.c			standard
 
 osfmk/kdp/ml/arm/kdp_machdep.c         optional        mach_kdp
 osfmk/kdp/ml/arm/kdp_vm.c              optional        mach_kdp
-
diff --git a/osfmk/console/arm/serial_console.c b/osfmk/console/arm/serial_console.c
index 993877c..b490a4d 100644
--- a/osfmk/console/arm/serial_console.c
+++ b/osfmk/console/arm/serial_console.c
@@ -35,6 +35,8 @@
 #include <console/video_console.h>
 #include <console/serial_protos.h>
 #include <kern/kalloc.h>
+#include <pexpert/arm/protos.h>
+#include <libkern/OSAtomic.h>
 
 static struct {
 	char	*buffer;
@@ -232,7 +234,7 @@ console_ring_try_empty(void)
 	/* Indicate that we're in the process of writing a block of data
 	 * to the console.
 	 */
-	atomic_incl(&console_output, 1);
+	OSAddAtomic(1, &console_output);
 	for (;;) {
 		char	ch;
 		if (!state)
@@ -244,7 +246,7 @@ console_ring_try_empty(void)
 			break;
 		_cnputc(ch);
 	}
-	atomic_decl(&console_output, 1);
+	OSAddAtomic(-1, &console_output);
 	simple_unlock(&console_ring.read_lock);
 }
 
diff --git a/osfmk/gssd/Makefile b/osfmk/gssd/Makefile
index bda924f..e3eb3b8 100644
--- a/osfmk/gssd/Makefile
+++ b/osfmk/gssd/Makefile
@@ -56,6 +56,9 @@ ${MIG_KUSRC} : gssd_mach.defs
 		-server  /dev/null		\
 		-sheader /dev/null		\
 		$<
+	# Workaround: MIG generates "() mig_strncpy" for c_string copyout
+	# on certain platforms. Replace with correct "mig_strncpy".
+	$(_v)sed -i 's/() mig_strncpy/mig_strncpy/g' $*.c
 
 include $(MakeInc_rule)
 include $(MakeInc_dir)
diff --git a/osfmk/kdp/ml/arm/kdp_machdep.c b/osfmk/kdp/ml/arm/kdp_machdep.c
index b2a7ebe..09f0428 100644
--- a/osfmk/kdp/ml/arm/kdp_machdep.c
+++ b/osfmk/kdp/ml/arm/kdp_machdep.c
@@ -48,6 +48,8 @@
 #include <arm/pmap.h>
 #include <kern/kalloc.h>
 
+extern void pmap_expand(pmap_t, vm_offset_t);
+
 #define KDP_TEST_HARNESS 0
 #if KDP_TEST_HARNESS
 #define dprintf(x) printf x
@@ -440,11 +442,11 @@ void kdp_machine_init(void) {
 
     vm_map_unlock(kernel_map);
 
-    debugger_ptep = pmap_pte(kernel_pmap, debugger_window_kva);
+    debugger_ptep = (pt_entry_t *)pmap_pte(kernel_pmap, debugger_window_kva);
 
     if (debugger_ptep == NULL) {
         pmap_expand(kernel_pmap, debugger_window_kva);
-        debugger_ptep = pmap_pte(kernel_pmap, debugger_window_kva);
+        debugger_ptep = (pt_entry_t *)pmap_pte(kernel_pmap, debugger_window_kva);
     }
 }
 
diff --git a/osfmk/kdp/ml/arm/kdp_vm.c b/osfmk/kdp/ml/arm/kdp_vm.c
index e05830f..4f8f7b5 100644
--- a/osfmk/kdp/ml/arm/kdp_vm.c
+++ b/osfmk/kdp/ml/arm/kdp_vm.c
@@ -125,7 +125,7 @@ static addr64_t kdp_vtophys(pmap_t pmap, addr64_t va)
     addr64_t pa;
     ppnum_t pp;
 
-    pp = pmap_find_phys_fvtp(pmap, va); /* Get the page number */
+    pp = pmap_find_phys(pmap, va); /* Get the page number */
     if (!pp)
         return 0;               /* Just return if no translation */
 
diff --git a/osfmk/kern/kmod.c b/osfmk/kern/kmod.c
index 160c4bd..d56fa7f 100644
--- a/osfmk/kern/kmod.c
+++ b/osfmk/kern/kmod.c
@@ -142,6 +142,11 @@ kmod_get_info(
     host_t host __unused,
     kmod_info_array_t * kmod_list KMOD_MIG_UNUSED,
     mach_msg_type_number_t * kmodCount KMOD_MIG_UNUSED);
+
+extern kern_return_t kext_get_kmod_info(
+    kmod_info_array_t * kmod_list,
+    mach_msg_type_number_t * kmodCount);
+
 kern_return_t
 kmod_get_info(
     host_t host __unused,
diff --git a/osfmk/kern/machine.h b/osfmk/kern/machine.h
index 6d46f2c..653c78d 100644
--- a/osfmk/kern/machine.h
+++ b/osfmk/kern/machine.h
@@ -100,6 +100,8 @@ extern void halt_cpu(void);
 extern void halt_all_cpus(
 					boolean_t           reboot);
 
+extern void Halt_system(void);
+
 extern char *machine_boot_info(
 					char                *buf,
 					vm_size_t           buf_len);
diff --git a/osfmk/kern/misc_protos.h b/osfmk/kern/misc_protos.h
index fa9a572..ddfe716 100644
--- a/osfmk/kern/misc_protos.h
+++ b/osfmk/kern/misc_protos.h
@@ -83,6 +83,8 @@ extern int copyin(
 	char                *kernel_addr,
 	vm_size_t           nbytes);
 
+extern void Halt_system(void);
+
 /* Move a NUL-terminated string from a user space to kernel space */
 extern int copyinstr(
 	const user_addr_t   user_addr,
diff --git a/osfmk/mach/arm/asm.h b/osfmk/mach/arm/asm.h
index 7f3b07f..84bed99 100644
--- a/osfmk/mach/arm/asm.h
+++ b/osfmk/mach/arm/asm.h
@@ -175,9 +175,15 @@
 /*
  * arg0: Register for thread pointer
  */
+#ifndef __linux__
 .macro READ_THREAD
 	mrc p15, 0, $0, c13, c0, 4  /* Read TPIDRPRW */
 .endmacro
+#else
+.macro READ_THREAD reg
+	mrc p15, 0, \reg, c13, c0, 4  /* Read TPIDRPRW */
+.endm
+#endif
 
 
 /* Macros for loading up addresses that are external to the .s file.
@@ -201,7 +207,7 @@
  *   LOAD_ADDR_GEN_DEF(arm_init_cpu)
  */
 
-#ifdef _ARM_ARCH_7
+#if defined(_ARM_ARCH_7) && !defined(__linux__)
 #define SLIDABLE 1
 #endif
 
diff --git a/osfmk/vm/vm_pageout.c b/osfmk/vm/vm_pageout.c
index 2bfd6e7..c95e6df 100644
--- a/osfmk/vm/vm_pageout.c
+++ b/osfmk/vm/vm_pageout.c
@@ -3022,7 +3022,8 @@ uint32_t vm_pageout_considered_page_last = 0;
  * called once per-second via "compute_averages"
  */
 void
-compute_pageout_gc_throttle()
+compute_pageout_gc_throttle(
+    void *arg __unused)
 {
 	if (vm_pageout_considered_page != vm_pageout_considered_page_last) {
 
diff --git a/pexpert/arm/common/pe_init.c b/pexpert/arm/common/pe_init.c
index 56f03c5..b18d155 100644
--- a/pexpert/arm/common/pe_init.c
+++ b/pexpert/arm/common/pe_init.c
@@ -42,6 +42,10 @@
 #include <kern/debug.h>
 #include "boot_images.h"
 
+extern boolean_t console_is_serial(void);
+extern void ovbcopy(const void *from, void *to, size_t len);
+extern void panic_ui_initialize(const unsigned char * system_clut);
+
 /* private globals */
 PE_state_t PE_state;
 
diff --git a/pexpert/arm/common/pe_serial.c b/pexpert/arm/common/pe_serial.c
index 4b93f24..4597b82 100644
--- a/pexpert/arm/common/pe_serial.c
+++ b/pexpert/arm/common/pe_serial.c
@@ -30,6 +30,7 @@
 #include <mach/mach_types.h>
 #include <pexpert/pexpert.h>
 #include <pexpert/arm/boot.h>
+#include <kern/debug.h>
 
 extern int pe_initialized;
 
diff --git a/pexpert/conf/files.arm b/pexpert/conf/files.arm
index 6918d22..c8ecbd0 100644
--- a/pexpert/conf/files.arm
+++ b/pexpert/conf/files.arm
@@ -18,4 +18,6 @@ pexpert/arm/pe_s5l8720x.c		standard
 pexpert/arm/pe_s5l8900x.c		standard
 pexpert/arm/pe_s5l8930x.c		standard
 pexpert/arm/pe_apq8060.c		standard
+pexpert/arm/pe_msm8916.c		standard
+pexpert/arm/pe_generic.c		standard
 pexpert/arm/pe_imx53.c			standard
diff --git a/pexpert/conf/files.arm64 b/pexpert/conf/files.arm64
index 3e70b9a..0c6bb72 100644
--- a/pexpert/conf/files.arm64
+++ b/pexpert/conf/files.arm64
@@ -6,3 +6,6 @@ pexpert/arm/common/pe_bootargs.c	standard
 pexpert/arm/common/pe_identify_machine.c	standard
 pexpert/arm/common/pe_socsupport.c	standard
 pexpert/arm/common/pe_armsupport.c	standard
+pexpert/arm/pe_generic_arm64.c		standard
+pexpert/arm/pe_msm8916.c		optional board_config_msm8916_harpia
+pexpert/arm/pe_msm8916_arm64.c		optional board_config_msm8916_harpia
